# 太迪杯团队 Git 协作指南

> **版本：v1.0**
> 适用仓库：https://github.com/smooth-QAQ/taidi_cup_B
> 最后更新：2026-04-19

---

## 📋 目录

1. [仓库信息](#仓库信息)
2. [快速接入（3分钟上手）](#快速接入3分钟上手)
3. [每日开发流程](#每日开发流程)
4. [分支命名规范](#分支命名规范)
5. [提交信息规范](#提交信息规范)
6. [Pull Request 流程](#pull-request-流程)
7. [反悔指南（重要！）](#反悔指南重要)
8. [常见问题](#常见问题)
9. [命令速查表](#命令速查表)

---

## 仓库信息

| 项目 | 信息 |
|------|------|
| **仓库地址** | https://github.com/smooth-QAQ/taidi_cup_B |
| **仓库状态** | 公开仓库（Public） |
| **主分支** | `main`（受保护，需审核后合并） |

---

## 快速接入（3分钟上手）

### 第一步：安装 Git

**Windows 用户**：
1. 下载 Git：https://git-scm.com/download/win
2. 安装时一路下一步即可

**验证安装**：
```bash
git --version
# 应该显示类似：git version 2.x.x
```

### 第二步：配置 Git

```bash
# 设置用户名（替换为你的名字）
git config --global user.name "你的名字"

# 设置邮箱
git config --global user.email "你的邮箱@example.com"

# 查看配置
git config --list
```

### 第三步：克隆仓库

```bash
# 进入工作目录
cd c:\cursor_Xiangmu

# 克隆仓库
git clone https://github.com/smooth-QAQ/taidi_cup_B.git

# 进入项目目录
cd taidi_cup_B
```

> **注意**：如果提示需要登录，输入你的 GitHub 用户名和密码（或 Personal Access Token）

### 第四步：创建功能分支

```bash
# 从 main 创建你的功能分支（替换为你的名字和功能）
git checkout -b feature/你的名字-功能描述

# 例如：
git checkout -b feature/lyc-登录模块
git checkout -b feature/zhang-数据处理
```

---

## 每日开发流程

### 早晨：同步最新代码

```bash
# 确保在 main 分支
git checkout main

# 拉取最新代码
git pull origin main

# 创建/切换到你的功能分支
git checkout feature/你的名字-功能描述
```

### 开发中：提交代码

```bash
# 1. 查看当前更改
git status

# 2. 添加要提交的文件
git add 文件名        # 添加单个文件
git add .             # 添加所有更改

# 3. 提交（写清楚做了什么）
git commit -m "feat: 添加用户登录功能"

# 4. 推送到远程
git push origin feature/你的名字-功能描述
```

### 完成功能后：创建 Pull Request

1. 在 GitHub 页面点击 **Compare & pull request**
2. 选择分支：`feature/xxx` → `main`
3. 填写信息后点击 **Create pull request**
4. 等待管理员审核

---

## 分支命名规范

| 类型 | 命名格式 | 示例 |
|------|----------|------|
| 功能开发 | `feature/名字-功能` | `feature/lyc-登录模块` |
| 紧急修复 | `hotfix/名字-问题` | `hotfix/lyc-修复登录bug` |
| 文档更新 | `docs/名字-内容` | `docs/lyc-更新README` |

> **规则**：名字用拼音首字母或英文，功能用中文简述，用 `-` 分隔

---

## 提交信息规范

### 格式

```
<类型>: <简短描述>

[可选的详细说明]
```

### 类型标识

| 标识 | 含义 | 示例 |
|------|------|------|
| `feat:` | 新功能 | `feat: 添加用户登录功能` |
| `fix:` | 修复bug | `fix: 修复登录超时问题` |
| `docs:` | 文档更新 | `docs: 更新README` |
| `refactor:` | 重构代码 | `refactor: 简化数据库查询` |
| `test:` | 测试相关 | `test: 添加单元测试` |
| `chore:` | 其他杂项 | `chore: 更新依赖版本` |

### 示例

```bash
git commit -m "feat: 添加用户注册功能"
git commit -m "fix: 修复数据库连接池泄漏"
git commit -m "docs: 更新操作指南"
```

---

## Pull Request 流程

### 创建 PR

1. **推送代码后**，GitHub 会显示 **Compare & pull request** 按钮，点击它
2. **填写 PR 信息**：
   - **Title**：`[feat] 用户登录模块`
   - **Description**：
     ```
     ## 做了什么
     - 添加了用户登录页面
     - 集成了后端API
     
     ## 测试情况
     - 本地测试通过
     ```
3. 点击 **Create pull request**

### 等待审核

- 管理员会收到通知
- 请等待审核，**不要**自行合并到 `main`
- 如果需要修改，管理员会留言

### 合并后

- 删除你的功能分支（可选）
- 继续下一个功能开发

---

## 反悔指南（重要！）

### 场景 1：提交了但还没 push，想取消

```bash
# 撤销最后一次提交，保留更改在本地
git reset --soft HEAD~1

# 或者完全丢弃这次提交
git reset --hard HEAD~1
```

### 场景 2：已 push，想撤回

```bash
# 方法1：撤销提交（推荐，保留历史）
git revert HEAD

# 方法2：强制回退（慎用！改变历史）
git reset --hard HEAD~1
git push --force
```

### 场景 3：合并 PR 后发现有问题

```bash
# 1. 找到合并的 commit ID
git log --oneline

# 2. 撤销合并
git revert -m 1 <merge-commit-id>

# 3. 推送
git push origin main
```

### 场景 4：想回到之前的任意版本

```bash
# 查看所有操作历史
git reflog

# 恢复到指定版本
git checkout <commit-id>
```

### 场景 5：文件改错了，想恢复

```bash
# 恢复单个文件
git restore filename.py

# 恢复所有文件
git restore .
```

---

## 常见问题

### Q1：push 时提示 "rejected"

```bash
# 先拉取远程最新代码
git pull origin main

# 如果有冲突，手动解决后重新提交
git add .
git commit -m "解决冲突"
git push
```

### Q2：冲突了怎么办？

Git 会标记冲突文件，打开文件查找 `<<<<<<<` 和 `>>>>>>>`：

```diff
<<<<<<< HEAD
+ 你的代码
=======
+ 别人的代码
>>>>>>> feature/xxx
```

保留需要的代码，删除标记，然后：
```bash
git add .
git commit -m "解决冲突"
git push
```

### Q3：不小心删除了本地分支

```bash
# 从远程恢复
git checkout -b feature/xxx origin/feature/xxx
```

### Q4：想看某个文件的历史

```bash
git log --oneline filename.py
git show <commit-id>:filename.py
```

---

## 命令速查表

### 基础操作

```bash
git status              # 查看当前状态
git diff                # 查看更改内容
git log --oneline       # 查看提交历史（简洁）
```

### 分支操作

```bash
git branch              # 查看本地分支
git branch -a           # 查看所有分支（包括远程）
git checkout 分支名     # 切换分支
git checkout -b 分支名  # 创建并切换到新分支
git branch -d 分支名    # 删除本地分支
```

### 同步操作

```bash
git pull                # 拉取并合并
git fetch               # 只拉取不合并
git push                # 推送
git push -u origin 分支  # 首次推送（设置上游分支）
```

### 撤销操作

```bash
git reset HEAD~1        # 撤销本地提交
git revert HEAD         # 创建撤销提交
git restore 文件名       # 恢复单个文件
git restore .           # 恢复所有文件
```

---

## 🎉 恭喜完成！

现在你已经掌握了团队协作的基本流程：

1. ✅ 克隆仓库到本地
2. ✅ 创建功能分支
3. ✅ 开发并提交代码
4. ✅ 创建 Pull Request
5. ✅ 知道如何反悔

**有问题及时在群里沟通，祝比赛顺利！🚀**

---

*维护者：smooth-QAQ*
*问题反馈：在 GitHub 提 Issue 或联系管理员*
